description: >
  terraform apply

parameters:
  additional_terraform_arguments:
    description: "String representation of additional arguments to pass to terraform apply"
    type: string
    default: ""
steps:
  - run:
      name: "Terraform Apply"
      command: <<include(scripts/terraform_apply.sh)>>
