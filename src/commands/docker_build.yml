description: >
  Docker-builds

parameters:
  directory:
    type: string
    default: "."
  dockerfile:
    description: "Dockerfile to override defaults with"
    type: string
    default: "Dockerfile"
steps:
  - run:
      name: "Docker Build"
      command: <<include(scripts/docker_build.sh)>>
