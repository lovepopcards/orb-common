description: >
  terraform Initialize

parameters:
  remote-config-key:
    description: "The key to use for the terraform remote config."
    type: string
steps:
  - run:
      name: "Configure Git for Terraform"
      command: <<include(scripts/terraform_init_configure.sh)>>
  - run:
      name: "Initialize Terraform"
      command: <<include(scripts/terraform_init_initialize.sh)>>
